feat(M1): 曲名跑马灯 + GitHub Pages 数据源 + 同步诊断

一、曲名自动左右循环滚动（你要求的）

- 新增 MarqueeText：一行显示，用 TextPainter 量出真实宽度，
  只有超出可用宽度时才启动动画；放得下就静止，不会平白动起来。
- 动作用 AnimationController.repeat(reverse: true)，
  一条 0 到 1 的直线动画镜像播放，等于「推到末尾再弹回起点」，首尾都能读到。
- 曲名走跑马灯（一行），曲师正常换行（最多 2 行）。
  这样 25 字曲名不会再把曲师挤掉，也不需要手动滑动。
- 为什么不用横向滚动条：卡片很窄，手动左右滑难以精确操作，
  还会和门页的上下滚动抢手势。
- 测量结果按 (text, style, 可用宽度) 缓存，不在 build 里反复 layout。

二、热更新的新数据源：GitHub Pages

- 你反馈三个源全部不可达（错误里报 raw.githack.com 域名解析失败）。
  实测这些域名在我的环境能解析且 jsDelivr 可拉取，所以问题是
  你的网络访问不到 GitHub 系域名。
- 但 *.github.io 是另一个域名、走另一套 CDN，在很多这类网络下能通。
  所以新增 .github/workflows/publish-pages.yml，在 data/ 变更时自动把
  四个 JSON 平铺发布到 Pages，app 的第一个源改为
  https://gzlxz190614.github.io/lvchecker/data/
- 注意 Pages 只能 serve 仓库根目录或 /docs，而数据在 data/ 下，
  所以发布时复制到 _site/data/ 以保持相同的相对路径。
- 源顺序：Pages -> jsDelivr -> raw.githubusercontent -> raw.githack

三、同步诊断（原来排查不了）

- 失败时记录每个源各自的失败原因，不再只留最后一个源的错误
  ——之前错误信息里只有 raw.githack.com，看不出其它源的情况。
- DNS 失败单独识别成「域名解析失败（xxx），本机网络访问不到这个域名」，
  比原始 SocketException 文本好读。
- 新增 probeSources()：逐个源做连通性探测。
- 设置页新增「测试各数据源的连通性」按钮，逐个显示 域名/结果/耗时，
  一眼看出是全部域名都不通，还是只有某一个。
  并说明：全部不通时热更新用不了，但 app 完全正常（一直用 APK 内置数据）。

四、其他

- README 更新数据源说明与排查步骤。
- tools/check_dart.py 的 4 条规则全部通过（含新增的第 4 条）。
